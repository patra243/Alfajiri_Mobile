<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@color/dfwhite"
    android:padding="20dp"
    android:weightSum="14"
    tools:context=".WelcomeActivity">

    <androidx.viewpager.widget.ViewPager
        android:id="@+id/viewPager"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="12">

    </androidx.viewpager.widget.ViewPager>

    <Button
        android:id="@+id/startBtn"
        android:layout_width="180dp"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_gravity="center"
        android:text="Get Started"
        android:textSize="14sp"
        android:textColor="@color/dfwhite"
        android:visibility="invisible"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="horizontal">

        <com.google.android.material.tabs.TabLayout
            android:id="@+id/icTab"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="2"
            app:tabGravity="center"
            app:tabIndicatorGravity="stretch"
            app:tabIconTint="@color/anti"
            android:layout_gravity="start"
            android:background="@color/dfwhite"
            app:tabBackground="@drawable/ic_shape_dot"
            app:tabIndicatorColor="@color/dfwhite">

        </com.google.android.material.tabs.TabLayout>

        <TextView
            android:id="@+id/txtNext"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="4"
            android:gravity="end"
            android:layout_marginBottom="12dp"
            android:layout_gravity="center"
            android:drawableRight="@drawable/ic_arrow_next"
            android:drawablePadding="5dp"
            android:drawableTint="@color/aspha"
            android:textAlignment="gravity"
            android:text="NEXT"
            android:textColor="@color/aspha"
            android:textSize="20sp"/>

    </LinearLayout>

</LinearLayout>